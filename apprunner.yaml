version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build
run:
  command: node dist/index.cjs
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production
